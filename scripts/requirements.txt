git+https://github.com/dougmet/yamlmd
git+https://github.com/open-sdg/sdg-build@2.3.0
frictionless==4.40.8
pydantic==1.*

# lxml arrives transitively with no upper bound. Version 6 declares support for
# the Python used by these workflows but publishes no wheel for it, so pip falls
# back to a source build that fails. Remove this pin only after moving the
# workflows to a newer Python.
lxml<6
